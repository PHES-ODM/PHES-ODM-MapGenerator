# Mapper Validator

`mapper_validator.py` validates the LinkML-Map YAML schemas (mappers) produced
by [make_mappers_cli.py](../README.md). It checks that every enumeration
mapping is complete, consistent, and free of typos — catching problems that
would otherwise silently produce incorrect data transformations.

Run it after `make_mappers_cli.py` to confirm that the generated mappers are
correct before using them to transform data.

## What it checks

For every `.yaml` mapper file in the `--mappers-dir` directory, the validator
performs five checks:

1. **Enum derivations exist** — every source slot whose range is an enumeration
   must have a corresponding `enum_derivation` block in the mapper. Missing
   blocks are reported as errors in the log.

2. **Missing source enum values** — every permissible value of a source
   enumeration must appear somewhere in the mapper's
   `permissible_value_derivations`. Values that are present in the source schema
   but absent from the mapper are written to
   `{tag}-missing_source_enum_values.csv`.

3. **Unrecognized source enum values** — enum values listed inside
   `permissible_value_derivations` that do not exist in the source schema (e.g.
   typos or stale values) are written to
   `{tag}-unrecognized_source_enum_values.csv`.

4. **Unrecognized target enum values** — enum values that a source value is
   mapped *to*, but that do not exist in the target schema, are written to
   `{tag}-unrecognized_target_enum_values.csv`.

5. **Empty permissible value derivations** — `enum_derivation` blocks that
   contain no `permissible_value_derivations` at all are written to
   `{tag}-empty_permissible_value_derivations.csv`.

6. **Unrecognized constant values** — slot derivations that use a quoted
   constant expression (`expr: 'someValue'`) where the constant is not a valid
   permissible value for the target slot are written to
   `{tag}-unrecognized_constant_values.csv`.

Output CSV files are only created when errors are found. If all checks pass, the
output directory is empty.

## Interpreting the results

Reported errors do not automatically mean the mapper must be changed. In some
cases, the downstream [PHES-ODM-Mapper](https://github.com/Big-Life-Lab/PHES-ODM-Mapper)
may handle missing mappings or supply default values itself. It is the
responsibility of the mapping configuration file author to review each reported
issue and decide whether it needs to be corrected in the mapper or whether it is
intentional and acceptable given how the downstream mapper will handle it.

## CLI options

**--mappers-dir** (Required)  
Directory containing the LinkML-Map schemas to validate. Every `.yaml` file in
the directory is validated.

**--source-schema** (Required)  
Path to the source LinkML schema that the mappers were generated from.

**--target-schema** (Required)  
Path to the target LinkML schema that the mappers map to.

**--output-dir** (Required)  
Directory where validation results (CSV files) are saved. Created automatically
if it does not exist.

**--tag** (Optional)  
String prepended to all output file names, followed by a dash. For example,
`--tag odm-v1-to-v3` produces files named
`odm-v1-to-v3-missing_source_enum_values.csv`, etc. Useful when running the
validator for multiple mappings into the same output directory.

**--simplify / --no-simplify** (Optional, default: `--simplify`)  
When enabled (the default), rows that differ only in the mapper file name are
merged into a single row. The `mapper` column lists all affected files separated
by semicolons, and a `numMappers` column shows how many files share the same
issue. This makes the reports much easier to read when many mapper files contain
the same problem. Pass `--no-simplify` to keep one row per mapper file.

## ODM v1 to ODM

Validates mappers generated by
[make_mappers_cli.py](../README.md#odm-v1-to-odm). Run `make_mappers_cli.py`
first to produce the mappers, then validate them.

```console
# Validate ODM v1 → ODM v3 mappers
python odm_map_maker/validate_mappers/mapper_validator.py \
    --mappers-dir ../gen/odm-v1-to-v3/mappers \
    --source-schema odm_map_maker/data/odm_v1/linkml/odm_v1.yaml \
    --target-schema odm_map_maker/data/odm_v3/linkml/odm_v3.yaml \
    --output-dir ../gen/validate/odm-v1-to-v3 \
    --tag odm-v1-to-v3 \
    --simplify

# Validate ODM v1 → ODM v2 mappers
python odm_map_maker/validate_mappers/mapper_validator.py \
    --mappers-dir ../gen/odm-v1-to-v2/mappers \
    --source-schema odm_map_maker/data/odm_v1/linkml/odm_v1.yaml \
    --target-schema odm_map_maker/data/odm_v2/linkml/odm_v2.yaml \
    --output-dir ../gen/validate/odm-v1-to-v2 \
    --tag odm-v1-to-v2 \
    --simplify
```

## NWSS to ODM

Validates mappers generated by
[make_mappers_cli.py](../README.md#nwss-to-odm). Three NWSS dictionary types are
supported: `reporting`, `public_concentration`, and `public_metric`. Substitute
the dictionary type name into the paths as shown below.

```console
# Validate NWSS reporting → ODM v3 mappers
python odm_map_maker/validate_mappers/mapper_validator.py \
    --mappers-dir ../gen/nwss-reporting-to-v3/mappers \
    --source-schema odm_map_maker/data/nwss_reporting/linkml/nwss_reporting.yaml \
    --target-schema odm_map_maker/data/odm_v3/linkml/odm_v3.yaml \
    --output-dir ../gen/validate/nwss-reporting-to-v3 \
    --tag nwss-reporting-to-v3 \
    --simplify

# Validate NWSS reporting → ODM v2 mappers
python odm_map_maker/validate_mappers/mapper_validator.py \
    --mappers-dir ../gen/nwss-reporting-to-v2/mappers \
    --source-schema odm_map_maker/data/nwss_reporting/linkml/nwss_reporting.yaml \
    --target-schema odm_map_maker/data/odm_v2/linkml/odm_v2.yaml \
    --output-dir ../gen/validate/nwss-reporting-to-v2 \
    --tag nwss-reporting-to-v2 \
    --simplify

# Validate NWSS public_concentration → ODM v3 mappers
python odm_map_maker/validate_mappers/mapper_validator.py \
    --mappers-dir ../gen/nwss-public_concentration-to-v3/mappers \
    --source-schema odm_map_maker/data/nwss_public_concentration/linkml/nwss_public_concentration.yaml \
    --target-schema odm_map_maker/data/odm_v3/linkml/odm_v3.yaml \
    --output-dir ../gen/validate/nwss-public_concentration-to-v3 \
    --tag nwss-public_concentration-to-v3 \
    --simplify

# Validate NWSS public_concentration → ODM v2 mappers
python odm_map_maker/validate_mappers/mapper_validator.py \
    --mappers-dir ../gen/nwss-public_concentration-to-v2/mappers \
    --source-schema odm_map_maker/data/nwss_public_concentration/linkml/nwss_public_concentration.yaml \
    --target-schema odm_map_maker/data/odm_v2/linkml/odm_v2.yaml \
    --output-dir ../gen/validate/nwss-public_concentration-to-v2 \
    --tag nwss-public_concentration-to-v2 \
    --simplify

# Validate NWSS public_metric → ODM v3 mappers
python odm_map_maker/validate_mappers/mapper_validator.py \
    --mappers-dir ../gen/nwss-public_metric-to-v3/mappers \
    --source-schema odm_map_maker/data/nwss_public_metric/linkml/nwss_public_metric.yaml \
    --target-schema odm_map_maker/data/odm_v3/linkml/odm_v3.yaml \
    --output-dir ../gen/validate/nwss-public_metric-to-v3 \
    --tag nwss-public_metric-to-v3 \
    --simplify

# Validate NWSS public_metric → ODM v2 mappers
python odm_map_maker/validate_mappers/mapper_validator.py \
    --mappers-dir ../gen/nwss-public_metric-to-v2/mappers \
    --source-schema odm_map_maker/data/nwss_public_metric/linkml/nwss_public_metric.yaml \
    --target-schema odm_map_maker/data/odm_v2/linkml/odm_v2.yaml \
    --output-dir ../gen/validate/nwss-public_metric-to-v2 \
    --tag nwss-public_metric-to-v2 \
    --simplify
```

## PHA4GE to ODM

Validates mappers generated by
[make_mappers_cli.py](../README.md#pha4ge-to-odm).

```console
# Validate PHA4GE → ODM v3 mappers
python odm_map_maker/validate_mappers/mapper_validator.py \
    --mappers-dir ../gen/pha4ge-to-v3/mappers \
    --source-schema odm_map_maker/data/pha4ge/linkml/pha4ge.yaml \
    --target-schema odm_map_maker/data/odm_v3/linkml/odm_v3.yaml \
    --output-dir ../gen/validate/pha4ge-to-v3 \
    --tag pha4ge-to-v3 \
    --simplify

# Validate PHA4GE → ODM v2 mappers
python odm_map_maker/validate_mappers/mapper_validator.py \
    --mappers-dir ../gen/pha4ge-to-v2/mappers \
    --source-schema odm_map_maker/data/pha4ge/linkml/pha4ge.yaml \
    --target-schema odm_map_maker/data/odm_v2/linkml/odm_v2.yaml \
    --output-dir ../gen/validate/pha4ge-to-v2 \
    --tag pha4ge-to-v2 \
    --simplify
```

## Output files

All output files are CSV. A file is created only when the corresponding
category of errors is found — no file means no errors of that type.

| File | Description |
|------|-------------|
| `{tag}-missing_source_enum_values.csv` | Source enum values that exist in the source schema but have no mapping in the mapper. |
| `{tag}-unrecognized_source_enum_values.csv` | Values listed as sources in `permissible_value_derivations` that are not valid enum values in the source schema. |
| `{tag}-unrecognized_target_enum_values.csv` | Values listed as targets in `permissible_value_derivations` that are not valid enum values in the target schema. |
| `{tag}-empty_permissible_value_derivations.csv` | Enum derivations that have no `permissible_value_derivations` block at all. |
| `{tag}-unrecognized_constant_values.csv` | Constant `expr` values in slot derivations that are not valid permissible values for the target slot. |

Each CSV contains columns identifying the source class, source slot, source enum
name, source enum value, target class, target slot, target enum name, and target
enum value, where applicable. When `--simplify` is active, a `numMappers` column
shows how many mapper files share the issue, and the `mapper` column lists all
of them separated by semicolons.
